#ifndef _UTILS_H
#define _UTILS_H
#include <stdio.h>
#include <stdlib.h>

void print_usage();
float sum(float, float);
float sub(float, float);
float mul(float, float);
float div(float, float);

#endif
